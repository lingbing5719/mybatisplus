<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mybatisplus.vod.mapper.ResourceRecordingMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.mybatisplus.vod.entity.ResourceRecording">
        <id column="Id" property="Id" />
        <result column="resourceId" property="resourceId" />
        <result column="playTime" property="playTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        Id, resourceId, playTime
    </sql>

</mapper>
